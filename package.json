{
  "name": "redis-server",
  "description": "Start and stop a Redis server.",
  "version": "0.2.0",
  "license": "MIT",
  "author": {
    "name": "Brandon Zacharie",
    "email": "brandon.zacharie@gmail.com"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BrandonZacharie/node-redis-server.git"
  },
  "keywords": [
    "redis",
    "server",
    "manager"
  ],
  "scripts": {
    "test": "./node_modules/.bin/mocha --bail test",
    "test+coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- --reporter dot",
    "lint": "./node_modules/.bin/jshint RedisServer.js",
    "ci": "npm run lint && npm run test+coverage && cat ./coverage/lcov.info | ./node_modules/.bin/coveralls"
  },
  "main": "./RedisServer.js",
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.15",
    "istanbul": "^0.4.5",
    "jshint": "^2.9.4",
    "mocha": "^3.1.2"
  }
}
